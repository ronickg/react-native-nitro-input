---
sidebar_position: 3
title: Text
description: 'mode="text": characters fade and scale between two strings, with the caret kept where you typed.'
---

import {Example} from '@site/src/components/Demo/Demo';
import {NitroInputTextDemo} from '@site/src/components/NitroInput/NitroInputDemo';

<Example
  code={`<MorphInput
  placeholder="Your name"
  fontSize={22}
  autoCapitalize="words"
  returnKeyType="done"
  onSubmitEditing={(name) => save(name)}
  style={{ width: '100%', height: 44, paddingHorizontal: 12 }}
/>`}>
  <NitroInputTextDemo />
</Example>

In `mode="text"` (the default) characters fade and scale in and out. Digits and separators slide only in `mode="number"`, unless `effect="slide"` or `effect="fade"` forces one style for everything.
