cmake_minimum_required(VERSION 3.22.1)
project(NitroRollingNumber)

set (PACKAGE_NAME NitroRollingNumber)
set (CMAKE_VERBOSE_MAKEFILE ON)
set (CMAKE_CXX_STANDARD 20)

# Define C++ library and add all sources
add_library(${PACKAGE_NAME} SHARED
        src/main/cpp/cpp-adapter.cpp
        src/main/cpp/JRollingEngine.cpp
        src/main/cpp/JGlyphMorph.cpp
        ../cpp/RollingEngine.cpp
        ../cpp/GlyphMorph.cpp
)

# Add Nitrogen specs :)
include(${CMAKE_SOURCE_DIR}/../nitrogen/generated/android/NitroRollingNumber+autolinking.cmake)

# Set up local includes
include_directories(
        "src/main/cpp"
        "../cpp"
)

find_library(LOG_LIB log)

# Link all libraries together
target_link_libraries(
        ${PACKAGE_NAME}
        ${LOG_LIB}
        android                                   # <-- Android core
)
