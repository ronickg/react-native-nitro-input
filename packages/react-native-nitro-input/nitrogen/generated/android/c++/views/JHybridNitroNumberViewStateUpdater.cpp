///
/// JHybridNitroNumberViewStateUpdater.cpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#include "JHybridNitroNumberViewStateUpdater.hpp"
#include "views/HybridNitroNumberViewComponent.hpp"
#include <NitroModules/NitroDefines.hpp>
#include <react/fabric/StateWrapperImpl.h>

namespace margelo::nitro::nitroinput::views {

using namespace facebook;
using ConcreteStateData = react::ConcreteState<HybridNitroNumberViewState>;

std::shared_ptr<const HybridNitroNumberViewProps> JHybridNitroNumberViewStateUpdater::getPropsFromStateWrapper(
    jni::alias_ref<JStateWrapper::javaobject> stateWrapper) {
  if (stateWrapper.get() == nullptr) {
    return nullptr;
  }
  // Get concrete StateWrapperImpl from passed StateWrapper interface object
  jobject rawStateWrapper = stateWrapper.get();
  if (!stateWrapper->isInstanceOf(react::StateWrapperImpl::javaClassStatic())) [[unlikely]] {
    throw std::runtime_error("StateWrapper is not a StateWrapperImpl");
  }
  auto stateWrapperImpl = jni::alias_ref<react::StateWrapperImpl::javaobject>{
    static_cast<react::StateWrapperImpl::javaobject>(rawStateWrapper)
  };
  std::shared_ptr<const react::State> state = stateWrapperImpl->cthis()->getState();
  if (state == nullptr) {
    return nullptr;
  }
  auto concreteState = std::static_pointer_cast<const ConcreteStateData>(state);
  const HybridNitroNumberViewState& data = concreteState->getData();
  const std::shared_ptr<const HybridNitroNumberViewProps>& props = data.getProps();
  if (props == nullptr) [[unlikely]] {
    throw std::runtime_error("HybridNitroNumberViewState's data doesn't contain any props!");
  }
  return props;
}

void JHybridNitroNumberViewStateUpdater::updateViewProps(jni::alias_ref<jni::JClass> /* class */,
                                           jni::alias_ref<JHybridNitroNumberViewSpec::JavaPart> javaView,
                                           jni::alias_ref<JStateWrapper::javaobject> newState,
                                           jni::alias_ref<JStateWrapper::javaobject> oldState) {
  std::shared_ptr<JHybridNitroNumberViewSpec> hybridView = javaView->getJHybridNitroNumberViewSpec();
  std::shared_ptr<const HybridNitroNumberViewProps> newProps = getPropsFromStateWrapper(newState);
  std::shared_ptr<const HybridNitroNumberViewProps> oldProps = getPropsFromStateWrapper(oldState);
  if (newProps == nullptr) [[unlikely]] {
    throw std::runtime_error("Current StateWrapper doesn't contain any props!");
  }

  // Update only props that differ from the previous State snapshot.
  if (oldProps == nullptr
        ? newProps->value.isProvided()
        : !newProps->value.hasSameValue(oldProps->value)) {
    hybridView->setValue(newProps->value.get());
  }
  if (oldProps == nullptr
        ? newProps->fractionDigits.isProvided()
        : !newProps->fractionDigits.hasSameValue(oldProps->fractionDigits)) {
    hybridView->setFractionDigits(newProps->fractionDigits.get());
  }
  if (oldProps == nullptr
        ? newProps->minimumIntegerDigits.isProvided()
        : !newProps->minimumIntegerDigits.hasSameValue(oldProps->minimumIntegerDigits)) {
    hybridView->setMinimumIntegerDigits(newProps->minimumIntegerDigits.get());
  }
  if (oldProps == nullptr
        ? newProps->groupingSeparator.isProvided()
        : !newProps->groupingSeparator.hasSameValue(oldProps->groupingSeparator)) {
    hybridView->setGroupingSeparator(newProps->groupingSeparator.get());
  }
  if (oldProps == nullptr
        ? newProps->decimalSeparator.isProvided()
        : !newProps->decimalSeparator.hasSameValue(oldProps->decimalSeparator)) {
    hybridView->setDecimalSeparator(newProps->decimalSeparator.get());
  }
  if (oldProps == nullptr
        ? newProps->prefix.isProvided()
        : !newProps->prefix.hasSameValue(oldProps->prefix)) {
    hybridView->setPrefix(newProps->prefix.get());
  }
  if (oldProps == nullptr
        ? newProps->suffix.isProvided()
        : !newProps->suffix.hasSameValue(oldProps->suffix)) {
    hybridView->setSuffix(newProps->suffix.get());
  }
  if (oldProps == nullptr
        ? newProps->duration.isProvided()
        : !newProps->duration.hasSameValue(oldProps->duration)) {
    hybridView->setDuration(newProps->duration.get());
  }
  if (oldProps == nullptr
        ? newProps->easing.isProvided()
        : !newProps->easing.hasSameValue(oldProps->easing)) {
    hybridView->setEasing(newProps->easing.get());
  }
  if (oldProps == nullptr
        ? newProps->bounce.isProvided()
        : !newProps->bounce.hasSameValue(oldProps->bounce)) {
    hybridView->setBounce(newProps->bounce.get());
  }
  if (oldProps == nullptr
        ? newProps->stagger.isProvided()
        : !newProps->stagger.hasSameValue(oldProps->stagger)) {
    hybridView->setStagger(newProps->stagger.get());
  }
  if (oldProps == nullptr
        ? newProps->transition.isProvided()
        : !newProps->transition.hasSameValue(oldProps->transition)) {
    hybridView->setTransition(newProps->transition.get());
  }
  if (oldProps == nullptr
        ? newProps->flashUpColor.isProvided()
        : !newProps->flashUpColor.hasSameValue(oldProps->flashUpColor)) {
    hybridView->setFlashUpColor(newProps->flashUpColor.get());
  }
  if (oldProps == nullptr
        ? newProps->flashDownColor.isProvided()
        : !newProps->flashDownColor.hasSameValue(oldProps->flashDownColor)) {
    hybridView->setFlashDownColor(newProps->flashDownColor.get());
  }
  if (oldProps == nullptr
        ? newProps->flashDuration.isProvided()
        : !newProps->flashDuration.hasSameValue(oldProps->flashDuration)) {
    hybridView->setFlashDuration(newProps->flashDuration.get());
  }
  if (oldProps == nullptr
        ? newProps->popOnChange.isProvided()
        : !newProps->popOnChange.hasSameValue(oldProps->popOnChange)) {
    hybridView->setPopOnChange(newProps->popOnChange.get());
  }
  if (oldProps == nullptr
        ? newProps->rollDirection.isProvided()
        : !newProps->rollDirection.hasSameValue(oldProps->rollDirection)) {
    hybridView->setRollDirection(newProps->rollDirection.get());
  }
  if (oldProps == nullptr
        ? newProps->revealState.isProvided()
        : !newProps->revealState.hasSameValue(oldProps->revealState)) {
    hybridView->setRevealState(newProps->revealState.get());
  }
  if (oldProps == nullptr
        ? newProps->revealStyle.isProvided()
        : !newProps->revealStyle.hasSameValue(oldProps->revealStyle)) {
    hybridView->setRevealStyle(newProps->revealStyle.get());
  }
  if (oldProps == nullptr
        ? newProps->revealDuration.isProvided()
        : !newProps->revealDuration.hasSameValue(oldProps->revealDuration)) {
    hybridView->setRevealDuration(newProps->revealDuration.get());
  }
  if (oldProps == nullptr
        ? newProps->revealBounce.isProvided()
        : !newProps->revealBounce.hasSameValue(oldProps->revealBounce)) {
    hybridView->setRevealBounce(newProps->revealBounce.get());
  }
  if (oldProps == nullptr
        ? newProps->revealGrow.isProvided()
        : !newProps->revealGrow.hasSameValue(oldProps->revealGrow)) {
    hybridView->setRevealGrow(newProps->revealGrow.get());
  }
  if (oldProps == nullptr
        ? newProps->revealStagger.isProvided()
        : !newProps->revealStagger.hasSameValue(oldProps->revealStagger)) {
    hybridView->setRevealStagger(newProps->revealStagger.get());
  }
  if (oldProps == nullptr
        ? newProps->revealMilestones.isProvided()
        : !newProps->revealMilestones.hasSameValue(oldProps->revealMilestones)) {
    hybridView->setRevealMilestones(newProps->revealMilestones.get());
  }
  if (oldProps == nullptr
        ? newProps->revealMilestoneHold.isProvided()
        : !newProps->revealMilestoneHold.hasSameValue(oldProps->revealMilestoneHold)) {
    hybridView->setRevealMilestoneHold(newProps->revealMilestoneHold.get());
  }
  if (oldProps == nullptr
        ? newProps->loading.isProvided()
        : !newProps->loading.hasSameValue(oldProps->loading)) {
    hybridView->setLoading(newProps->loading.get());
  }
  if (oldProps == nullptr
        ? newProps->shimmerColor.isProvided()
        : !newProps->shimmerColor.hasSameValue(oldProps->shimmerColor)) {
    hybridView->setShimmerColor(newProps->shimmerColor.get());
  }
  if (oldProps == nullptr
        ? newProps->shimmerDuration.isProvided()
        : !newProps->shimmerDuration.hasSameValue(oldProps->shimmerDuration)) {
    hybridView->setShimmerDuration(newProps->shimmerDuration.get());
  }
  if (oldProps == nullptr
        ? newProps->fontSize.isProvided()
        : !newProps->fontSize.hasSameValue(oldProps->fontSize)) {
    hybridView->setFontSize(newProps->fontSize.get());
  }
  if (oldProps == nullptr
        ? newProps->prefixFontSize.isProvided()
        : !newProps->prefixFontSize.hasSameValue(oldProps->prefixFontSize)) {
    hybridView->setPrefixFontSize(newProps->prefixFontSize.get());
  }
  if (oldProps == nullptr
        ? newProps->suffixFontSize.isProvided()
        : !newProps->suffixFontSize.hasSameValue(oldProps->suffixFontSize)) {
    hybridView->setSuffixFontSize(newProps->suffixFontSize.get());
  }
  if (oldProps == nullptr
        ? newProps->affixAlign.isProvided()
        : !newProps->affixAlign.hasSameValue(oldProps->affixAlign)) {
    hybridView->setAffixAlign(newProps->affixAlign.get());
  }
  if (oldProps == nullptr
        ? newProps->prefixAlign.isProvided()
        : !newProps->prefixAlign.hasSameValue(oldProps->prefixAlign)) {
    hybridView->setPrefixAlign(newProps->prefixAlign.get());
  }
  if (oldProps == nullptr
        ? newProps->suffixAlign.isProvided()
        : !newProps->suffixAlign.hasSameValue(oldProps->suffixAlign)) {
    hybridView->setSuffixAlign(newProps->suffixAlign.get());
  }
  if (oldProps == nullptr
        ? newProps->letterSpacing.isProvided()
        : !newProps->letterSpacing.hasSameValue(oldProps->letterSpacing)) {
    hybridView->setLetterSpacing(newProps->letterSpacing.get());
  }
  if (oldProps == nullptr
        ? newProps->prefixSpacing.isProvided()
        : !newProps->prefixSpacing.hasSameValue(oldProps->prefixSpacing)) {
    hybridView->setPrefixSpacing(newProps->prefixSpacing.get());
  }
  if (oldProps == nullptr
        ? newProps->suffixSpacing.isProvided()
        : !newProps->suffixSpacing.hasSameValue(oldProps->suffixSpacing)) {
    hybridView->setSuffixSpacing(newProps->suffixSpacing.get());
  }
  if (oldProps == nullptr
        ? newProps->prefixOffset.isProvided()
        : !newProps->prefixOffset.hasSameValue(oldProps->prefixOffset)) {
    hybridView->setPrefixOffset(newProps->prefixOffset.get());
  }
  if (oldProps == nullptr
        ? newProps->suffixOffset.isProvided()
        : !newProps->suffixOffset.hasSameValue(oldProps->suffixOffset)) {
    hybridView->setSuffixOffset(newProps->suffixOffset.get());
  }
  if (oldProps == nullptr
        ? newProps->tabularNums.isProvided()
        : !newProps->tabularNums.hasSameValue(oldProps->tabularNums)) {
    hybridView->setTabularNums(newProps->tabularNums.get());
  }
  if (oldProps == nullptr
        ? newProps->adjustsFontSizeToFit.isProvided()
        : !newProps->adjustsFontSizeToFit.hasSameValue(oldProps->adjustsFontSizeToFit)) {
    hybridView->setAdjustsFontSizeToFit(newProps->adjustsFontSizeToFit.get());
  }
  if (oldProps == nullptr
        ? newProps->minimumFontScale.isProvided()
        : !newProps->minimumFontScale.hasSameValue(oldProps->minimumFontScale)) {
    hybridView->setMinimumFontScale(newProps->minimumFontScale.get());
  }
  if (oldProps == nullptr
        ? newProps->allowFontScaling.isProvided()
        : !newProps->allowFontScaling.hasSameValue(oldProps->allowFontScaling)) {
    hybridView->setAllowFontScaling(newProps->allowFontScaling.get());
  }
  if (oldProps == nullptr
        ? newProps->maxFontSizeMultiplier.isProvided()
        : !newProps->maxFontSizeMultiplier.hasSameValue(oldProps->maxFontSizeMultiplier)) {
    hybridView->setMaxFontSizeMultiplier(newProps->maxFontSizeMultiplier.get());
  }
  if (oldProps == nullptr
        ? newProps->fontWeight.isProvided()
        : !newProps->fontWeight.hasSameValue(oldProps->fontWeight)) {
    hybridView->setFontWeight(newProps->fontWeight.get());
  }
  if (oldProps == nullptr
        ? newProps->fontFamily.isProvided()
        : !newProps->fontFamily.hasSameValue(oldProps->fontFamily)) {
    hybridView->setFontFamily(newProps->fontFamily.get());
  }
  if (oldProps == nullptr
        ? newProps->color.isProvided()
        : !newProps->color.hasSameValue(oldProps->color)) {
    hybridView->setColor(newProps->color.get());
  }
  if (oldProps == nullptr
        ? newProps->textAlign.isProvided()
        : !newProps->textAlign.hasSameValue(oldProps->textAlign)) {
    hybridView->setTextAlign(newProps->textAlign.get());
  }
  if (oldProps == nullptr
        ? newProps->rightToLeft.isProvided()
        : !newProps->rightToLeft.hasSameValue(oldProps->rightToLeft)) {
    hybridView->setRightToLeft(newProps->rightToLeft.get());
  }
  if (oldProps == nullptr
        ? newProps->onSizeChange.isProvided()
        : !newProps->onSizeChange.hasSameValue(oldProps->onSizeChange)) {
    hybridView->setOnSizeChange(newProps->onSizeChange.get());
  }
  if (oldProps == nullptr
        ? newProps->onRevealEnd.isProvided()
        : !newProps->onRevealEnd.hasSameValue(oldProps->onRevealEnd)) {
    hybridView->setOnRevealEnd(newProps->onRevealEnd.get());
  }
  if (oldProps == nullptr
        ? newProps->onRevealMilestone.isProvided()
        : !newProps->onRevealMilestone.hasSameValue(oldProps->onRevealMilestone)) {
    hybridView->setOnRevealMilestone(newProps->onRevealMilestone.get());
  }

  // Update hybridRef if it changed
  if (oldProps == nullptr
        ? newProps->hybridRef.isProvided()
        : !newProps->hybridRef.hasSameValue(oldProps->hybridRef)) {
    // hybridRef changed - call it with new this
    const auto& maybeFunc = newProps->hybridRef.get();
    if (maybeFunc.has_value()) {
      maybeFunc.value()(hybridView);
    }
  }
}

} // namespace margelo::nitro::nitroinput::views
