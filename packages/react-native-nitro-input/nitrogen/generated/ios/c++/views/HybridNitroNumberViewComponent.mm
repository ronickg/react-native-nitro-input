///
/// HybridNitroNumberViewComponent.mm
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#import "HybridNitroNumberViewComponent.hpp"
#import <memory>
#import <react/renderer/componentregistry/ComponentDescriptorProvider.h>
#import <React/RCTViewComponentView.h>
#import <React/RCTComponentViewFactory.h>
#import <React/UIView+ComponentViewProtocol.h>
#import <NitroModules/NitroDefines.hpp>
#import <UIKit/UIKit.h>

#import "HybridNitroNumberViewSpecSwift.hpp"
#import "NitroInput-Swift-Cxx-Umbrella.hpp"

#if __has_include(<cxxreact/ReactNativeVersion.h>)
#include <cxxreact/ReactNativeVersion.h>
#if REACT_NATIVE_VERSION_MINOR >= 82
#define ENABLE_RCT_COMPONENT_VIEW_INVALIDATE
#endif
#endif

using namespace facebook;
using namespace margelo::nitro::nitroinput;
using namespace margelo::nitro::nitroinput::views;

/**
 * Represents the React Native View holder for the Nitro "NitroNumberView" HybridView.
 */
@interface HybridNitroNumberViewComponent: RCTViewComponentView
+ (BOOL)shouldBeRecycled;
@end

@implementation HybridNitroNumberViewComponent {
  std::shared_ptr<HybridNitroNumberViewSpecSwift> _hybridView;
  BOOL _didDropView;
}

+ (void) load {
  [super load];
  [RCTComponentViewFactory.currentComponentViewFactory registerComponentViewClass:[HybridNitroNumberViewComponent class]];
}

+ (react::ComponentDescriptorProvider) componentDescriptorProvider {
  return react::concreteComponentDescriptorProvider<HybridNitroNumberViewComponentDescriptor>();
}

- (instancetype) init {
  if (self = [super init]) {
    _props = HybridNitroNumberViewShadowNode::defaultSharedProps();
    std::shared_ptr<HybridNitroNumberViewSpec> hybridView = NitroInput::NitroInputAutolinking::createNitroNumberView();
    _hybridView = std::dynamic_pointer_cast<HybridNitroNumberViewSpecSwift>(hybridView);
    [self updateView];
  }
  return self;
}

- (void) updateView {
  // 1. Get Swift part
  NitroInput::HybridNitroNumberViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Get UIView*
  void* viewUnsafe = swiftPart.getView();
  UIView* view = (__bridge_transfer UIView*) viewUnsafe;

  // 3. Update RCTViewComponentView's [contentView]
  [self setContentView:view];
}

- (void) notifyOnDropView {
  // A recycled component can later be invalidated. Notify only once per mount.
  if (_didDropView) {
    return;
  }
  NitroInput::HybridNitroNumberViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();
  swiftPart.onDropView();
  _didDropView = YES;
}

- (void) updateProps:(const std::shared_ptr<const react::Props>&)props
            oldProps:(const std::shared_ptr<const react::Props>&)oldProps {
  // A props update marks a newly mounted or still-active component.
  _didDropView = NO;

  // 1. Downcast props
  const auto& newViewProps = *std::static_pointer_cast<const HybridNitroNumberViewProps>(props);
  const auto* oldViewProps = static_cast<const HybridNitroNumberViewProps*>(oldProps.get());
  NitroInput::HybridNitroNumberViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Update only props that differ from the previous Props snapshot.
  const bool hasTransactionPropChanges = oldViewProps == nullptr
      ? newViewProps.hasAnyProvidedProps()
      : !newViewProps.hasSameProps(*oldViewProps);
  if (hasTransactionPropChanges) {
    swiftPart.beforeUpdate();

    // value: number
    if (oldViewProps == nullptr
          ? newViewProps.value.isProvided()
          : !newViewProps.value.hasSameValue(oldViewProps->value)) {
      swiftPart.setValue(newViewProps.value.get());
    }
    // fractionDigits: optional
    if (oldViewProps == nullptr
          ? newViewProps.fractionDigits.isProvided()
          : !newViewProps.fractionDigits.hasSameValue(oldViewProps->fractionDigits)) {
      swiftPart.setFractionDigits(newViewProps.fractionDigits.get());
    }
    // minimumIntegerDigits: optional
    if (oldViewProps == nullptr
          ? newViewProps.minimumIntegerDigits.isProvided()
          : !newViewProps.minimumIntegerDigits.hasSameValue(oldViewProps->minimumIntegerDigits)) {
      swiftPart.setMinimumIntegerDigits(newViewProps.minimumIntegerDigits.get());
    }
    // groupingSeparator: optional
    if (oldViewProps == nullptr
          ? newViewProps.groupingSeparator.isProvided()
          : !newViewProps.groupingSeparator.hasSameValue(oldViewProps->groupingSeparator)) {
      swiftPart.setGroupingSeparator(newViewProps.groupingSeparator.get());
    }
    // decimalSeparator: optional
    if (oldViewProps == nullptr
          ? newViewProps.decimalSeparator.isProvided()
          : !newViewProps.decimalSeparator.hasSameValue(oldViewProps->decimalSeparator)) {
      swiftPart.setDecimalSeparator(newViewProps.decimalSeparator.get());
    }
    // prefix: optional
    if (oldViewProps == nullptr
          ? newViewProps.prefix.isProvided()
          : !newViewProps.prefix.hasSameValue(oldViewProps->prefix)) {
      swiftPart.setPrefix(newViewProps.prefix.get());
    }
    // suffix: optional
    if (oldViewProps == nullptr
          ? newViewProps.suffix.isProvided()
          : !newViewProps.suffix.hasSameValue(oldViewProps->suffix)) {
      swiftPart.setSuffix(newViewProps.suffix.get());
    }
    // duration: optional
    if (oldViewProps == nullptr
          ? newViewProps.duration.isProvided()
          : !newViewProps.duration.hasSameValue(oldViewProps->duration)) {
      swiftPart.setDuration(newViewProps.duration.get());
    }
    // easing: optional
    if (oldViewProps == nullptr
          ? newViewProps.easing.isProvided()
          : !newViewProps.easing.hasSameValue(oldViewProps->easing)) {
      swiftPart.setEasing(newViewProps.easing.get());
    }
    // bounce: optional
    if (oldViewProps == nullptr
          ? newViewProps.bounce.isProvided()
          : !newViewProps.bounce.hasSameValue(oldViewProps->bounce)) {
      swiftPart.setBounce(newViewProps.bounce.get());
    }
    // stagger: optional
    if (oldViewProps == nullptr
          ? newViewProps.stagger.isProvided()
          : !newViewProps.stagger.hasSameValue(oldViewProps->stagger)) {
      swiftPart.setStagger(newViewProps.stagger.get());
    }
    // transition: optional
    if (oldViewProps == nullptr
          ? newViewProps.transition.isProvided()
          : !newViewProps.transition.hasSameValue(oldViewProps->transition)) {
      swiftPart.setTransition(newViewProps.transition.get());
    }
    // flashUpColor: optional
    if (oldViewProps == nullptr
          ? newViewProps.flashUpColor.isProvided()
          : !newViewProps.flashUpColor.hasSameValue(oldViewProps->flashUpColor)) {
      swiftPart.setFlashUpColor(newViewProps.flashUpColor.get());
    }
    // flashDownColor: optional
    if (oldViewProps == nullptr
          ? newViewProps.flashDownColor.isProvided()
          : !newViewProps.flashDownColor.hasSameValue(oldViewProps->flashDownColor)) {
      swiftPart.setFlashDownColor(newViewProps.flashDownColor.get());
    }
    // flashDuration: optional
    if (oldViewProps == nullptr
          ? newViewProps.flashDuration.isProvided()
          : !newViewProps.flashDuration.hasSameValue(oldViewProps->flashDuration)) {
      swiftPart.setFlashDuration(newViewProps.flashDuration.get());
    }
    // popOnChange: optional
    if (oldViewProps == nullptr
          ? newViewProps.popOnChange.isProvided()
          : !newViewProps.popOnChange.hasSameValue(oldViewProps->popOnChange)) {
      swiftPart.setPopOnChange(newViewProps.popOnChange.get());
    }
    // rollDirection: optional
    if (oldViewProps == nullptr
          ? newViewProps.rollDirection.isProvided()
          : !newViewProps.rollDirection.hasSameValue(oldViewProps->rollDirection)) {
      swiftPart.setRollDirection(newViewProps.rollDirection.get());
    }
    // revealState: optional
    if (oldViewProps == nullptr
          ? newViewProps.revealState.isProvided()
          : !newViewProps.revealState.hasSameValue(oldViewProps->revealState)) {
      swiftPart.setRevealState(newViewProps.revealState.get());
    }
    // revealStyle: optional
    if (oldViewProps == nullptr
          ? newViewProps.revealStyle.isProvided()
          : !newViewProps.revealStyle.hasSameValue(oldViewProps->revealStyle)) {
      swiftPart.setRevealStyle(newViewProps.revealStyle.get());
    }
    // revealDuration: optional
    if (oldViewProps == nullptr
          ? newViewProps.revealDuration.isProvided()
          : !newViewProps.revealDuration.hasSameValue(oldViewProps->revealDuration)) {
      swiftPart.setRevealDuration(newViewProps.revealDuration.get());
    }
    // revealBounce: optional
    if (oldViewProps == nullptr
          ? newViewProps.revealBounce.isProvided()
          : !newViewProps.revealBounce.hasSameValue(oldViewProps->revealBounce)) {
      swiftPart.setRevealBounce(newViewProps.revealBounce.get());
    }
    // revealGrow: optional
    if (oldViewProps == nullptr
          ? newViewProps.revealGrow.isProvided()
          : !newViewProps.revealGrow.hasSameValue(oldViewProps->revealGrow)) {
      swiftPart.setRevealGrow(newViewProps.revealGrow.get());
    }
    // revealStagger: optional
    if (oldViewProps == nullptr
          ? newViewProps.revealStagger.isProvided()
          : !newViewProps.revealStagger.hasSameValue(oldViewProps->revealStagger)) {
      swiftPart.setRevealStagger(newViewProps.revealStagger.get());
    }
    // revealMilestones: optional
    if (oldViewProps == nullptr
          ? newViewProps.revealMilestones.isProvided()
          : !newViewProps.revealMilestones.hasSameValue(oldViewProps->revealMilestones)) {
      swiftPart.setRevealMilestones(newViewProps.revealMilestones.get());
    }
    // revealMilestoneHold: optional
    if (oldViewProps == nullptr
          ? newViewProps.revealMilestoneHold.isProvided()
          : !newViewProps.revealMilestoneHold.hasSameValue(oldViewProps->revealMilestoneHold)) {
      swiftPart.setRevealMilestoneHold(newViewProps.revealMilestoneHold.get());
    }
    // loading: optional
    if (oldViewProps == nullptr
          ? newViewProps.loading.isProvided()
          : !newViewProps.loading.hasSameValue(oldViewProps->loading)) {
      swiftPart.setLoading(newViewProps.loading.get());
    }
    // shimmerColor: optional
    if (oldViewProps == nullptr
          ? newViewProps.shimmerColor.isProvided()
          : !newViewProps.shimmerColor.hasSameValue(oldViewProps->shimmerColor)) {
      swiftPart.setShimmerColor(newViewProps.shimmerColor.get());
    }
    // shimmerDuration: optional
    if (oldViewProps == nullptr
          ? newViewProps.shimmerDuration.isProvided()
          : !newViewProps.shimmerDuration.hasSameValue(oldViewProps->shimmerDuration)) {
      swiftPart.setShimmerDuration(newViewProps.shimmerDuration.get());
    }
    // fontSize: optional
    if (oldViewProps == nullptr
          ? newViewProps.fontSize.isProvided()
          : !newViewProps.fontSize.hasSameValue(oldViewProps->fontSize)) {
      swiftPart.setFontSize(newViewProps.fontSize.get());
    }
    // prefixFontSize: optional
    if (oldViewProps == nullptr
          ? newViewProps.prefixFontSize.isProvided()
          : !newViewProps.prefixFontSize.hasSameValue(oldViewProps->prefixFontSize)) {
      swiftPart.setPrefixFontSize(newViewProps.prefixFontSize.get());
    }
    // suffixFontSize: optional
    if (oldViewProps == nullptr
          ? newViewProps.suffixFontSize.isProvided()
          : !newViewProps.suffixFontSize.hasSameValue(oldViewProps->suffixFontSize)) {
      swiftPart.setSuffixFontSize(newViewProps.suffixFontSize.get());
    }
    // affixAlign: optional
    if (oldViewProps == nullptr
          ? newViewProps.affixAlign.isProvided()
          : !newViewProps.affixAlign.hasSameValue(oldViewProps->affixAlign)) {
      swiftPart.setAffixAlign(newViewProps.affixAlign.get());
    }
    // prefixAlign: optional
    if (oldViewProps == nullptr
          ? newViewProps.prefixAlign.isProvided()
          : !newViewProps.prefixAlign.hasSameValue(oldViewProps->prefixAlign)) {
      swiftPart.setPrefixAlign(newViewProps.prefixAlign.get());
    }
    // suffixAlign: optional
    if (oldViewProps == nullptr
          ? newViewProps.suffixAlign.isProvided()
          : !newViewProps.suffixAlign.hasSameValue(oldViewProps->suffixAlign)) {
      swiftPart.setSuffixAlign(newViewProps.suffixAlign.get());
    }
    // adjustsFontSizeToFit: optional
    if (oldViewProps == nullptr
          ? newViewProps.adjustsFontSizeToFit.isProvided()
          : !newViewProps.adjustsFontSizeToFit.hasSameValue(oldViewProps->adjustsFontSizeToFit)) {
      swiftPart.setAdjustsFontSizeToFit(newViewProps.adjustsFontSizeToFit.get());
    }
    // minimumFontScale: optional
    if (oldViewProps == nullptr
          ? newViewProps.minimumFontScale.isProvided()
          : !newViewProps.minimumFontScale.hasSameValue(oldViewProps->minimumFontScale)) {
      swiftPart.setMinimumFontScale(newViewProps.minimumFontScale.get());
    }
    // allowFontScaling: optional
    if (oldViewProps == nullptr
          ? newViewProps.allowFontScaling.isProvided()
          : !newViewProps.allowFontScaling.hasSameValue(oldViewProps->allowFontScaling)) {
      swiftPart.setAllowFontScaling(newViewProps.allowFontScaling.get());
    }
    // maxFontSizeMultiplier: optional
    if (oldViewProps == nullptr
          ? newViewProps.maxFontSizeMultiplier.isProvided()
          : !newViewProps.maxFontSizeMultiplier.hasSameValue(oldViewProps->maxFontSizeMultiplier)) {
      swiftPart.setMaxFontSizeMultiplier(newViewProps.maxFontSizeMultiplier.get());
    }
    // fontWeight: optional
    if (oldViewProps == nullptr
          ? newViewProps.fontWeight.isProvided()
          : !newViewProps.fontWeight.hasSameValue(oldViewProps->fontWeight)) {
      swiftPart.setFontWeight(newViewProps.fontWeight.get());
    }
    // fontFamily: optional
    if (oldViewProps == nullptr
          ? newViewProps.fontFamily.isProvided()
          : !newViewProps.fontFamily.hasSameValue(oldViewProps->fontFamily)) {
      swiftPart.setFontFamily(newViewProps.fontFamily.get());
    }
    // color: optional
    if (oldViewProps == nullptr
          ? newViewProps.color.isProvided()
          : !newViewProps.color.hasSameValue(oldViewProps->color)) {
      swiftPart.setColor(newViewProps.color.get());
    }
    // textAlign: optional
    if (oldViewProps == nullptr
          ? newViewProps.textAlign.isProvided()
          : !newViewProps.textAlign.hasSameValue(oldViewProps->textAlign)) {
      swiftPart.setTextAlign(newViewProps.textAlign.get());
    }
    // rightToLeft: optional
    if (oldViewProps == nullptr
          ? newViewProps.rightToLeft.isProvided()
          : !newViewProps.rightToLeft.hasSameValue(oldViewProps->rightToLeft)) {
      swiftPart.setRightToLeft(newViewProps.rightToLeft.get());
    }
    // onSizeChange: optional
    if (oldViewProps == nullptr
          ? newViewProps.onSizeChange.isProvided()
          : !newViewProps.onSizeChange.hasSameValue(oldViewProps->onSizeChange)) {
      swiftPart.setOnSizeChange(newViewProps.onSizeChange.get());
    }
    // onRevealEnd: optional
    if (oldViewProps == nullptr
          ? newViewProps.onRevealEnd.isProvided()
          : !newViewProps.onRevealEnd.hasSameValue(oldViewProps->onRevealEnd)) {
      swiftPart.setOnRevealEnd(newViewProps.onRevealEnd.get());
    }
    // onRevealMilestone: optional
    if (oldViewProps == nullptr
          ? newViewProps.onRevealMilestone.isProvided()
          : !newViewProps.onRevealMilestone.hasSameValue(oldViewProps->onRevealMilestone)) {
      swiftPart.setOnRevealMilestone(newViewProps.onRevealMilestone.get());
    }

    // Update hybridRef if it changed
    if (oldViewProps == nullptr
          ? newViewProps.hybridRef.isProvided()
          : !newViewProps.hybridRef.hasSameValue(oldViewProps->hybridRef)) {
      // hybridRef changed - call it with new this
      const auto& maybeFunc = newViewProps.hybridRef.get();
      if (maybeFunc.has_value()) {
        maybeFunc.value()(_hybridView);
      }
    }

    swiftPart.afterUpdate();
  }

  // 3. Continue in base class
  [super updateProps:props oldProps:oldProps];
}

+ (BOOL)shouldBeRecycled {
  return NitroInput::NitroInputAutolinking::isNitroNumberViewRecyclable();
}

- (void)prepareForRecycle {
  [self notifyOnDropView];
  [super prepareForRecycle];
  NitroInput::HybridNitroNumberViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();
  swiftPart.maybePrepareForRecycle();
}

#ifdef ENABLE_RCT_COMPONENT_VIEW_INVALIDATE
- (void)invalidate {
  [self notifyOnDropView];
  [super invalidate];
}
#endif

@end
